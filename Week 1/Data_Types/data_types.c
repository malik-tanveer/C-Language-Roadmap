#include <stdio.h>

int main()
{
    // integear mean that all is Numbers
    int a, b;

    a = 10;
    b = 20;
    // Evrone you use a integer and print is used a %i or %d
    printf("This is a Integers \n a = %i And b = %d \n", a, b);

    // float mean that your marks is represnent a this type 50.98

    float c = 10.21;
    float d = 30.9090;

    // for flaot cna you used a %f
    printf("This is a Float \n c = %f And d = %f \n", c, d);

    // This is a char mean that character

    char e = 'A'; // All of time used a you Char and decalre is single Qutation ''
    // char f_1 = 'apple'; // Not used a this is thorw a error use a Single Word
    char f = 'B';

    // for charcter to you use a %c

    printf("This is a Char (Charcater) \n e = %c And f = %c", e, f);
    return 0;
}