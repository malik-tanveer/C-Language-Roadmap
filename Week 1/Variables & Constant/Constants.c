#include <stdio.h>

#define PI 3.14159
int main()
{
    // This is a Constant's & is a 2 Types of 
    // 1. const keyword to declare
    // 2. #define keyword to declare

    // Constant's is a Fixed value

    const int MAX_USERS = 100; // MAX_USERS is a constant

    printf("Maximum users allowed: %d\n", MAX_USERS);


    printf("Value of PI: %f\n", PI);
    return 0;
}